const assert = require('assert');

describe('Example Unit Test', () => {
	describe('greeting', () => {
		it('is "Hello World!"', () => {
			assert.equal('Hello World!', 'Hello World!');
		});
	});
});
